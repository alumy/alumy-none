{
    "config": {
        "ghooks": {
            "commit-msg": "validate-commit-msg",
            "pre-push": "./pre_push.sh"
        },
        "commitizen": {
            "path": "cz-conventional-changelog"
        }
    },
    "scripts": {
        "commit": "git-cz",
        "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s -r 0"
    },
    "dependencies": {
        "ghooks": "^2.0.4"
    }
}
