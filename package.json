{
    "config": {
        "ghooks": {
            "commit-msg": "validate-commit-msg",
            "pre-push": "./cmake_gen.sh gcc.cmake Debug 1 && cd build && make && make test"
        },
        "commitizen": {
            "path": "cz-conventional-changelog"
        }
    },
    "scripts": {
        "commit": "git-cz",
        "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s -r 0"
    },
    "dependencies": {
        "ghooks": "^2.0.4"
    }
}
